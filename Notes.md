need to add a subfile for the requirements. cause when we do the update if in the install im adding some requirements like iptables cause its not installed by default we need to add it in  a file that the update script will read and update its installed app based on that.

in settings page when we do the upload and we validate for restore capabilities. we should pop the toast on restore capabilities validated not on clicking the restore button